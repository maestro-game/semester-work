<#include "base.ftl">
<@html "profile" "${owner.name} ${owner.surname}">
    <div class="profile">
        <h1 class="profile__name">${owner.name}</h1>
        <h1 class="profile__surname">${owner.surname}</h1>
    </div>

</@>
